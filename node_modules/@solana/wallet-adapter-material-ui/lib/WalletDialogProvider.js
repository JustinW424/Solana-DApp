import React, { useState } from 'react';
import { WalletDialogContext } from './useWalletDialog';
import { WalletDialog } from './WalletDialog';
export const WalletDialogProvider = ({ children }) => {
    const [open, setOpen] = useState(false);
    return (React.createElement(WalletDialogContext.Provider, { value: {
            open,
            setOpen,
        } },
        children,
        React.createElement(WalletDialog, null)));
};
//# sourceMappingURL=WalletDialogProvider.js.map